# irenemr-prueba-redradix

## Build Setup

```bash
# You can create a codespace on Github's repository. Go to Code > codespaces > create a new one and on the terminal run
$ npm install
$ npm run build
$ npm run start

# To run the project locally
$ npm install
$ npm run build
$ npm run start

# To run linter use
$ npm run lint
```
